# Cline Rules — LeetCode Hot 100 速通项目

## 📁 文件操作规范

### 1. Solution.java（主工作文件）

- **路径**: `src/main/java/com/leetcode/solutions/Solution.java`
- **开头注释**（Javadoc）必须包含：
  - 题号 + 标题（英文 + 中文）
  - 完整题目描述
  - 所有示例（输入/输出/解释）
  - 约束条件
  - 核心套路标签
  - 时间/空间复杂度
- **main 方法**必须包含测试用例（至少 3 个：2 个常规 + 1 个边界），直接打印输出结果，用户目视验证
- **测试策略**：直接用 Solution.java 的 main 方法测试，不额外引入 JUnit 测试框架

### 2. 题解迁移文件

- **路径**: `src/main/java/com/leetcode/solutions/LC{题号}_{题目名}.java`
- 评审通过且 A 级后，将 Solution.java 中的代码**完整迁移**至此
- 保留完整 Javadoc 描述（与 Solution.java 一致）
- Solution.java 清空为模板，准备下一题

### 3. 计划更新

- **文件**: `docs/plans/hot100-speedrun.md`
- 每道题完成后必须更新：
  - 评级列（A / ⬜）
  - 首刷日
  - 回顾轮次标记（R1/R2/R3/R4）
  - 分块进度
  - 总进度

---

## 📋 刷题持久化流程

```
用户要求刷某题
   ↓
1. AI 在 Solution.java 放置题目（完整 Javadoc + 测试 main）
2. 用户写代码
3. 用户通知 AI 评审
4. AI 编译运行验证（mvn compile exec:java 运行 main 方法）
5. 通过 → 评 A 级，迁移到 LC{题号}_{题目名}.java
    未通过 → 给提示，用户修改后重试
6. AI 更新 hot100-speedrun.md（评级/首刷日/进度）
```

---

## ⚠️ 养成模式

- Solution.java 的 Javadoc = **完整描述**，禁止省略号截断
- 迁移到 LC{题号}.java 时保留完整 Javadoc
- 所有文件名格式：`LC{题号}_{英文题目名}.java`（无空格）